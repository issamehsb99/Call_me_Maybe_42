[project]
name = "call-me-maybe"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "flake8>=7.3.0",
    "llm-sdk>=1.0.1",
    "mypy>=2.3.0",
    "numpy>=2.2.6",
    "pydantic>=2.13.4",
]

[tool.uv.sources]
llm_sdk = { path = "./llm_sdk" }
