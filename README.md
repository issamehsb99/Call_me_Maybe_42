# Call_me_Maybe_42
Call Me Maybe converts natural language prompts into valid JSON function calls using a small LLM and constrained decoding. It selects the correct function, extracts typed arguments, and ensures structured, machine-readable output for AI agents and API-based systems.
